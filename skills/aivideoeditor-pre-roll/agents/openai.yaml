interface:
  display_name: "AIVideoEditor Pre-roll"
  short_description: "Create pre-roll videos with required logo assets and selectable voices"
  default_prompt: "Use $aivideoeditor-pre-roll to create or render a pre-roll video with a real logo asset and optional voice choices."
